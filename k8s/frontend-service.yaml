apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: asi-dashboard
  labels:
    app: frontend
spec:
  type: LoadBalancer  # Use ClusterIP if you have an Ingress controller
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: frontend

