apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: asi-dashboard
type: Opaque
stringData:
  JWT_SECRET: "your-secret-key-change-in-production"  # Change this in production!
  DATABASE_PASSWORD: "root"  # Should match postgres-secret

